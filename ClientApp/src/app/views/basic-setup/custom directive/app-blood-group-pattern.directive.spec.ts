import { AppBloodGroupPatternDirective } from './app-blood-group-pattern.directive';

describe('AppBloodGroupPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBloodGroupPatternDirective();
    expect(directive).toBeTruthy();
  });
});
