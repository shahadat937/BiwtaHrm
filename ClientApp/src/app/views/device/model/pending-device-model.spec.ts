import { PendingDeviceModel } from './pending-device-model';

describe('PendingDeviceModel', () => {
  it('should create an instance', () => {
    expect(new PendingDeviceModel()).toBeTruthy();
  });
});
