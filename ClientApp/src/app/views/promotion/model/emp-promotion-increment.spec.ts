import { EmpPromotionIncrement } from './emp-promotion-increment';

describe('EmpPromotionIncrement', () => {
  it('should create an instance', () => {
    expect(new EmpPromotionIncrement()).toBeTruthy();
  });
});
