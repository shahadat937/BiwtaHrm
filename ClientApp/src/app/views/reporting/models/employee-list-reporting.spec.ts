import { EmployeeListReporting } from './employee-list-reporting';

describe('EmployeeListReporting', () => {
  it('should create an instance', () => {
    expect(new EmployeeListReporting()).toBeTruthy();
  });
});
