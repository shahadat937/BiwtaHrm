import { ModuleFeatureByRole } from './module-feature-by-role';

describe('ModuleFeatureByRole', () => {
  it('should create an instance', () => {
    expect(new ModuleFeatureByRole()).toBeTruthy();
  });
});
