import { PrlRetirementReporting } from './prl-retirement-reporting';

describe('PrlRetirementReporting', () => {
  it('should create an instance', () => {
    expect(new PrlRetirementReporting()).toBeTruthy();
  });
});
