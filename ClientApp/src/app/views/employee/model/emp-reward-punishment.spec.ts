import { EmpRewardPunishment } from './emp-reward-punishment';

describe('RewardPunishment', () => {
  it('should create an instance', () => {
    expect(new EmpRewardPunishment()).toBeTruthy();
  });
});
