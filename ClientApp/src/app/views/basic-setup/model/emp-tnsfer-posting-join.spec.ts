import { EmpTnsferPostingJoin } from './emp-tnsfer-posting-join';

describe('EmpTnsferPostingJoin', () => {
  it('should create an instance', () => {
    expect(new EmpTnsferPostingJoin()).toBeTruthy();
  });
});
