import { AttendanceDeviceModel } from './attendance-device-model';

describe('AttendanceDeviceModel', () => {
  it('should create an instance', () => {
    expect(new AttendanceDeviceModel()).toBeTruthy();
  });
});
