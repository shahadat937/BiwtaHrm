import { NotificationReadBy } from './notification-read-by';

describe('NotificationReadBy', () => {
  it('should create an instance', () => {
    expect(new NotificationReadBy()).toBeTruthy();
  });
});
