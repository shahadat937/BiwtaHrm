import { EmpCountOnReportingDto } from './emp-count-on-reporting-dto';

describe('EmpCountOnReportingDto', () => {
  it('should create an instance', () => {
    expect(new EmpCountOnReportingDto()).toBeTruthy();
  });
});
