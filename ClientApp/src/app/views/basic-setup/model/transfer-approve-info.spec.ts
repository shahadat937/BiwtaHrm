import { TransferApproveInfo } from './transfer-approve-info';

describe('TransferApproveInfo', () => {
  it('should create an instance', () => {
    expect(new TransferApproveInfo()).toBeTruthy();
  });
});
