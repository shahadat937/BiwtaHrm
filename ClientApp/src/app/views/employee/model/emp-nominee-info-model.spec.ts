import { EmpNomineeInfoModel } from './emp-nominee-info-model';

describe('EmpNomineeInfoModel', () => {
  it('should create an instance', () => {
    expect(new EmpNomineeInfoModel()).toBeTruthy();
  });
});
