import { AttendanceRecordModel } from './attendance-record-model';

describe('AttendanceRecordModel', () => {
  it('should create an instance', () => {
    expect(new AttendanceRecordModel()).toBeTruthy();
  });
});
