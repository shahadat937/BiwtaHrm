import { EmpTransferPosting } from './emp-transfer-posting';

describe('EmpTransferPosting', () => {
  it('should create an instance', () => {
    expect(new EmpTransferPosting()).toBeTruthy();
  });
});
