import { RewardPunishmentType } from './reward-punishment-type';

describe('RewardPunishmentType', () => {
  it('should create an instance', () => {
    expect(new RewardPunishmentType()).toBeTruthy();
  });
});
